# goal-cost

Estimate token spend for Grok Build `/goal` sessions.

```bash
npx @cobusgreyling/goal-cost --pattern tests-green --level G2
npx @cobusgreyling/goal-cost --list
```

Uses `patterns/registry.yaml` cost blocks. Pair with `goal-budget.md` turn caps.