name: Release goal-cost

on:
  push:
    tags:
      - 'goal-cost-v*'

permissions:
  contents: read
  id-token: write

jobs:
  test-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'
          cache-dependency-path: tools/goal-cost/package-lock.json
      - name: Install, build, test
        working-directory: tools/goal-cost
        run: |
          npm ci
          npm run build
          npm test
      - name: Publish to npm
        working-directory: tools/goal-cost
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}